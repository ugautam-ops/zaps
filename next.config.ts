// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // You can add Next.js options here (NOT tailwind options)
};

export default nextConfig;
